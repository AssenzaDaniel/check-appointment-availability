# check-appointment-availability
Service that checks if new appointments have been opened in the civil registry and send push notifications via Telegram, in order to notify the user so that they can make the appointment.
